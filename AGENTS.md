# Synthetic release validation

Run `npm test` for the complete independent test suite. Preserve the required CI checks and main-branch protection. Changes, branch pushes, pull requests and squash merges must stay within the explicitly authorized certification attempt and frozen repository target.
